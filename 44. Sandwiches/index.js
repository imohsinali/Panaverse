function make_sandwich(arr) {
  for (let i = 0; i < arr.length; i++) {
    console.log(`Sandwich: ${arr[i]}`);
  }
}

make_sandwich(["egg", "bread", "mayonnaise"]);
make_sandwich(["salad", "bread", "mayonnaise", "ketchup"]);
make_sandwich(["egg", "bread", "mayonnaise"]);
